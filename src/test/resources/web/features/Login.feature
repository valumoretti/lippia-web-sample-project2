Feature: Login

  #Scenario: Failed login with correct username and empty password.
   # When the client clicks on the My Account menu
    #And enters "valumorettiarias@gmail.com" in the username textbox
    #And leaves the password textbox empty
    #And clicks on the Login button
    #Then an appropriate error message (e.g., "Invalid password") should be displayed
    #And the user should be prompted to enter their login credentials again

  #Scenario: Failed login with empty username and valid password.
   # When the client clicks on the My Account menu
    #And leaves the username textbox empty
    #And enters "contraseniasegura" in the password textbox
    #And clicks on the Login button
    #Then an appropriate error message (e.g., "Invalid password") should be displayed
    #And the user should be prompted to enter their login credentials again

  #Scenario: Failed login with empty username and empty password.
    #When the client clicks on the My Account menu
    #And leaves the username textbox empty
    #And leaves the password textbox empty
    #And clicks on the Login button
    #Then an appropriate error message (e.g., "Invalid password") should be displayed
    #And the user should be prompted to enter their login credentials again